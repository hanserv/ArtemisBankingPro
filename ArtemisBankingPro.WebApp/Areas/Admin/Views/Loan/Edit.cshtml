@model ModifyLoanRateViewModel
@{
    ViewData["Title"] = "Edit loan interest rate";
}

<div>
    <div class="d-flex justify-content-between gap-2 mb-3">
        <a asp-area="Admin" asp-controller="Loan" asp-action="Index" class="btn btn-outline-dark fw-semibold">
            <i class="bi bi-arrow-left"></i> Go back
        </a>
    </div>

    <div class="card shadow-sm border-0">
        <div class="card-header bg-dark text-white">
            <h2 class="fs-5 m-0">
                <i class="bi bi-cash-stack"></i> Edit loan interest rate
            </h2>
        </div>
        <div class="card-body">
            <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>

            <form asp-area="Admin" asp-controller="Loan" asp-action="Edit" method="post" class="row g-3">
                <input type="hidden" asp-for="LoanId" />

                <div class="col-12 col-md-6">
                    <label asp-for="AnnualInterestRate" class="form-label">Annual interest rate</label>
                    <div class="input-group">
                        <input asp-for="AnnualInterestRate" class="form-control" step="0.01" />
                        <span class="input-group-text">%</span>
                    </div>
                    <span asp-validation-for="AnnualInterestRate" class="text-danger"></span>
                    <div class="form-text">This change only applies to future pending installments.</div>
                </div>

                <div class="col-12 d-flex justify-content-end mt-4">
                    <button type="submit" class="btn btn-dark px-5 fw-semibold">
                        <i class="bi bi-check-lg"></i> Update rate
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>